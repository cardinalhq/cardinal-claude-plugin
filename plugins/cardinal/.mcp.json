{
  "cardinal": {
    "type": "http",
    "url": "${CARDINAL_MCP_URL}",
    "headers": {
      "X-CardinalHQ-API-Key": "${CARDINAL_MCP_API_KEY}"
    }
  },
  "cardinal-code": {
    "type": "stdio",
    "command": "${CLAUDE_PLUGIN_ROOT}/bin/cardinal-serena"
  }
}
